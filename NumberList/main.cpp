#include "NumberList.h"
#include <iostream>
using namespace std;

int main()
{
	// replace all of the following code; this is just test
	// code from the book
	NumberList list;

	list.appendNode(10);
	list.appendNode(20);
	list.appendNode(30);
	list.appendNode(40);
	list.displayList();
}